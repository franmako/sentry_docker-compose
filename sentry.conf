upstream sentry {
    server sentry_sentry:9000;
}

server {
	listen   80; ## listen for ipv4; this line is default and implied
	listen   [::]:80 default; ## listen for ipv6

	server_name sentry.franmako.me;

	location /sentry {
		proxy_pass "http://sentry";
	}
}